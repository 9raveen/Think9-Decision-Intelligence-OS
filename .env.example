# Copy to .env and fill in. Never commit the real .env file.

# Used in later phases (agent layer / LangGraph) — not required for the
# current data-and-retrieval foundation phase.
ANTHROPIC_API_KEY=

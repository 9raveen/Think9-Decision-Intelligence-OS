pydantic>=2.6
scikit-learn>=1.4
numpy>=1.26
pytest>=8.0
qdrant-client>=1.9
langgraph>=0.2
groq>=0.11

# Added in later phase:
# streamlit
# sentence-transformers (blocked in this sandbox — see src/ingestion/semantic_embed.py)
# Added in later phase:
# streamlit
# sentence-transformers (blocked in this sandbox — see src/ingestion/semantic_embed.py)

